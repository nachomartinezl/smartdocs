# OpenAI API Key for text embedding and LLM-based extraction/schema generation
OPENAI_API_KEY="sk-YOUR_OPENAI_API_KEY"

# Path to the ChromaDB database directory
# Defaults to ./chroma_db if not set
CHROMA_PATH="./chroma_db"

# Concurrency settings for resource-intensive operations
# OCR_CONCURRENCY is used by build_index.py
OCR_CONCURRENCY=4
# Concurrency for vector_service.py (OpenAI API calls)
# VECTOR_SERVICE_CONCURRENCY=5 # This is set internally in vector_service.py, but good to be aware of

# LLM Models (Optional - defaults are set in the scripts)
# SCHEMA_GEN_MODEL="gpt-4o" # Used in generate_schemas.py
# EXTRACTION_MODEL="gpt-3.5-turbo" # Used in app/services/llm_service.py
# EMBED_MODEL="text-embedding-3-small" # Used in app/services/vector_service.py
